/*!
 * Action Tools - Implementation helpers
 *
 * All action tool implementations live in main.rs (#[tool_router] impl block).
 * This module is reserved for future extraction of shared helper logic.
 *
 * Key/action mappings are defined inline in main.rs (android_press_key,
 * android_global_action) to keep them co-located with their callers.
 */
